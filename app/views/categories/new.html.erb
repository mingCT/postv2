<h3 style="color:white">Create a new category.</h3>

<%= render 'shared/errors', obj: @category %>

<div class= "well well-small">
<%= form_for @category do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br>
	<%= f.submit class: "btn btn-primary" %>
<% end %>
</div>